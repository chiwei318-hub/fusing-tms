<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>富詠運輸 - 司機管理後台</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h2 class="mb-4">🚛 司機管理與核可名單</h2>
        
        <div class="card mb-4 shadow-sm">
            <div class="card-header bg-primary text-white">核可新加盟司機 (加入白名單)</div>
            <div class="card-body">
                <form action="/admin/add_whitelist" method="POST" class="row g-3">
                    <div class="col-md-5">
                        <input type="text" name="name" class="form-control" placeholder="司機姓名" required>
                    </div>
                    <div class="col-md-5">
                        <input type="text" name="phone" class="form-control" placeholder="手機號碼 (例如: 0912345678)" required>
                    </div>
                    <div class="col-md-2">
                        <button type="submit" class="btn btn-success w-100">核可加入</button>
                    </div>
                </form>
            </div>
        </div>

        <div class="card shadow-sm">
            <div class="card-header bg-dark text-white">目前在職司機狀態 (LINE 派單名單)</div>
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>姓名</th>
                            <th>手機號碼</th>
                            <th>LINE 狀態</th>
                            <th>在職狀態</th>
                            <th>操作</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for d in drivers %}
                        <tr>
                            <td>{{ d.name }}</td>
                            <td>{{ d.phone }}</td>
                            <td>
                                {% if d.line_uid %}
                                <span class="badge bg-success">已綁定 (可收單)</span>
                                {% else %}
                                <span class="badge bg-warning text-dark">未綁定 (收不到單)</span>
                                {% endif %}
                            </td>
                            <td>
                                {% if d.is_active %}
                                <span class="text-success">● 在職中</span>
                                {% else %}
                                <span class="text-danger">○ 已離職/停權</span>
                                {% endif %}
                            </td>
                            <td>
                                {% if d.is_active %}
                                <a href="/admin/deactivate/{{ d.line_uid }}" class="btn btn-sm btn-outline-danger" onclick="return confirm('確定要剔除此司機嗎？剔除後他將收不到任何派單。')">一鍵剔除</a>
                                {% else %}
                                <span class="text-muted">已停權</span>
                                {% endif %}
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>